---
title: "Chat Conversations"
api: "GET https://api-v2.opencopilot.so/backend/chat/:copilot_id"
description: "Retrieve paginated chat conversations, including if the conversatin was a handoff and the sentiment with the summary."
---

<Warning>
This endpoint is protected by the same JWT that the user uses to call other APIs. The response will include data for the specified chatbot, along with other metadata information.
</Warning>


<Warning>
This endpoint points to the (v2) version of the API. The (v2) version of the API is the latest version and is recommended for all new development. Please update your code to use the (v2) version of the API.
</Warning>

This endpoint allows you to retrieve all conversations for a specific Copilot ID. The response includes the conversation id, sentiment, and reason for handoff (if applicable).

The endpoint is paginated, and you can specify the `page` and `limit` query parameters to control the amount of data returned.

<RequestExample>
```bash Example Request
curl --location 'https://api-v2.opencopilot.so/backend/chat/:copilot_id' \
  --header 'Authorization: Bearer TOKEN' \ 
  --header 'Content-Type: application/json' 
```
</RequestExample>

<ResponseExample>
```json Response
[
  {
    "id": "string",
    "copilot_id": "string",
    "assignee_id": 0,
    "channel": "string",
    "status": 0,
    "last_message_at": "2024-06-04T07:10:38.748Z",
    "last_message": "string",
    "is_notification_enabled": true,
    "handoff": true,
    "summary": "string",
    "short_token": "string",
    "classification": "string",
    "handoff_sentiment": {},
    "email_thread_id": "string",
    "email": "string",
    "meta": {
      "title": "string",
      "whatsappToNumber": "string",
      "recordingUrl": "string",
      "title_generation_type": {},
      "summary": "string",
      "phoneNumber": "string"
    },
    "mobileNumber": "string",
    "updated_at": "2024-06-04T07:10:38.748Z",
    "created_at": "2024-06-04T07:10:38.748Z"
  }
]
```
</ResponseExample>

The request includes the following optional query parameters:
- `page`: The page number to retrieve (default is 1).
- `limit`: The number of items to retrieve per page (default is 10).
- `sortBy`: The field to sort by (default is `created_at`).
- `sortDirection`: The direction to sort (default is `ASC`) (`ASC` or `DESC`).


The response includes the following fields:

- `id`: The unique identifier for the conversation.
- `created_at`: The date and time the conversation was created.
- `handoff_sentiment`: The sentiment of the conversation at the time of handoff.
- `handoff_summary`: A summary of the conversation at the time of handoff.
- `handoff`: A boolean value indicating whether the conversation was handed off to a human agent.
- `classification`: The classification of the conversation (based on the user's provided categories and tags).
- `summary`: A summary of the conversation.
- `short_token`: A short token that can be used to identify the conversation.
- `status`: The status of the conversation (0 for active, 1 for resolved, 2 for cloesd).
- `is_notification_enabled`: A boolean value indicating whether notifications are enabled for the conversation.
- `last_message_at`: The date and time of the last message in the conversation.
- `last_message`: The last message in the conversation.
- `meta`: Metadata about the conversation (such as title, recording URL, and phone number).
- `mobileNumber`: The mobile number associated with the conversation (in case of phone-based conversations).
- `email`: The email associated with the conversation.
- `email_thread_id`: The ID of the email thread associated with the conversation (in case of email-based conversations).

You can use the `offset` and `limit` parameters to control the pagination of the response. For example, to retrieve the first 10 messages, you can use the following request:

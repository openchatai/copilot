# templates/dashboard/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dashboard
  labels:
    app: {{ .Release.Name }}-dashboard
spec:
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
  selector:
    app: {{ .Release.Name }}-dashboard
status:
  loadBalancer: {}
